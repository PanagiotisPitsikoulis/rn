{
  "name": "expo-full-stack-mobile-starter",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "packageManager": "bun@1.3.9",
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/core/*",
    "packages/headless/*",
    "packages/native/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:server": "bun --filter @expo-starter/server dev",
    "dev:mobile": "bun --filter @expo-starter/mobile dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "seed": "bun --filter @expo-starter/server seed",
    "db:schema": "bun --filter @expo-starter/server db:schema",
    "db:seed": "bun --filter @expo-starter/server db:seed"
  },
  "devDependencies": {
    "@types/react": "~19.2.15",
    "typescript": "~6.0.3",
    "turbo": "^2.5.4"
  }
}
